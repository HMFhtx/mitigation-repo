{
  "vulnerability": "API2-2023",
  "owasp_tag": "Broken Authentication",
  "subcategory": "Unsigned JWT (alg: none) Accepted",
  "steps": [
    {
      "id": 1,
      "vulnerability_name": "Block alg:none in workshop JWT decode — reject tokens before claims extraction",
      "description": "workshop/utils/jwt.py decodes JWTs with verify_signature=False. A defense-in-depth check rejects any token whose header declares alg:none before local decoding.",
      "file_path": "services/workshop/utils/jwt.py",
      "search_code": "                if response_status_code == status.HTTP_200_OK:\n                    decoded = jwt.decode(token, options={\"verify_signature\": False})",
      "replace_code": "                if response_status_code == status.HTTP_200_OK:\n                    _header = jwt.get_unverified_header(token)\n                    if _header.get(\"alg\", \"none\").lower() == \"none\":\n                        raise jwt.exceptions.InvalidAlgorithmError(\"alg:none tokens are not accepted\")\n                    decoded = jwt.decode(token, options={\"verify_signature\": False})"
    }
  ],
  "verification": [
    {
      "id": 1,
      "description": "Forge an alg:none token for an existing user and attempt to access the identity dashboard — must be rejected with 401",
      "curl": "NONE_TOKEN=$(python3 -c \"import base64,json; h=base64.urlsafe_b64encode(json.dumps({'alg':'none','typ':'JWT'}).encode()).rstrip(b'=').decode(); p=base64.urlsafe_b64encode(json.dumps({'sub':'user@example.com','role':'ROLE_USER'}).encode()).rstrip(b'=').decode(); print(f'{h}.{p}.')\"); curl -s -w '\\nHTTP_STATUS:%{http_code}' http://167.99.136.89:8888/identity/api/v2/user/dashboard -H \"Authorization: Bearer $NONE_TOKEN\"",
      "expect_status": 401,
      "expect_body_excludes": "email"
    },
    {
      "id": 2,
      "description": "Verify that the identity token verify endpoint rejects an alg:none token",
      "curl": "NONE_TOKEN=$(python3 -c \"import base64,json; h=base64.urlsafe_b64encode(json.dumps({'alg':'none','typ':'JWT'}).encode()).rstrip(b'=').decode(); p=base64.urlsafe_b64encode(json.dumps({'sub':'user@example.com'}).encode()).rstrip(b'=').decode(); print(f'{h}.{p}.')\"); curl -s -w '\\nHTTP_STATUS:%{http_code}' -X POST http://167.99.136.89:8888/identity/api/auth/verify -H 'Content-Type: application/json' -d \"{\\\"token\\\":\\\"$NONE_TOKEN\\\"}\"",
      "expect_status": 401,
      "expect_body_excludes": "true"
    }
  ]
}
